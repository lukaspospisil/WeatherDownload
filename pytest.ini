[pytest]
# The repo includes a `tmp/` scratch area used by some dev scripts. In this
# environment it may contain directories with restricted permissions, which can
# break test collection if pytest recurses into it.
norecursedirs = tmp

